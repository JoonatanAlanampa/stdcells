out/*.sp
out/*.log
out/*.ys
out/*.yslog
out/*.out
__pycache__/
harden/runs/
# stray foundry PDK artifacts copied in by local magic runs (zero-foundry policy)
flow/sky130.tech
# local KLayout DRC reports for the offbin gate-check (regenerated by run_offbin_drc.py)
flow/offbin/*.lyrdb
# v3 Phase 2 scratch: ngspice wrdata dumps + reharden-compare temp netlists/libs
# + the regenerable abc/hardening derivatives (xcheck_liberty.py rebuilds them).
# Kept: flow/v3/*.py|png|json|md and the deliverable out/own_devphys_xcheck.lib
out/*.txt
out/_cmp_*
out/own_devphys_xcheck_abc.lib
out/own_devphys_xcheck_hardening.lib
